# Web UI Subproject

This folder will contain the React/Vite UI for Autocodex.

## Rules
- Use TypeScript strict mode.
- No `any` types.
- Keep API calls in a single client module.

## Commands
- Dev: `npm run dev`
- Build: `npm run build`

## Performance
- Prefer memoized selectors and virtualization for large lists.
