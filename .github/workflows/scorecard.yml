name: scorecard

on:
  branch_protection_rule:
  schedule:
    - cron: "30 1 * * 0"
  push:
    branches: [main]

permissions: read-all

jobs:
  analysis:
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    runs-on: ubuntu-latest
    steps:
      - name: Skip scorecard (private repo without token)
        if: ${{ github.event.repository.private == true && secrets.SCORECARD_READ_TOKEN == '' }}
        run: echo "Scorecard skipped: private repo without SCORECARD_READ_TOKEN."
      - name: Checkout
        if: ${{ github.event.repository.private == false || secrets.SCORECARD_READ_TOKEN != '' }}
        uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Scorecard analysis
        if: ${{ github.event.repository.private == false || secrets.SCORECARD_READ_TOKEN != '' }}
        uses: ossf/scorecard-action@v2.4.3
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true
          repo_token: ${{ secrets.SCORECARD_READ_TOKEN || github.token }}
      - name: Upload results
        if: ${{ github.event.repository.private == false || secrets.SCORECARD_READ_TOKEN != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: scorecard-results
          path: results.sarif
          if-no-files-found: error
      - name: Upload SARIF
        if: ${{ github.event.repository.private == false || secrets.SCORECARD_READ_TOKEN != '' }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
