# Golden Task Catalog

1. Basic autonomy run emits valid ACTIONS JSON and completes.
2. Coordinator bead mode runs multiple ready beads without overlap.
3. Harness preflight succeeds in healthy environment.
4. High-impact mode with council GREEN + critic GO allows completion.
5. High-impact mode blocks when council verdict missing.
6. Lifecycle events include required metadata fields.

Required per-scenario evidence:
- `run_id`
- `context_pack_id` (or equivalent run context reference)
- gate matrix summary
- artifact pointers
